{
  "name": "invoiz-desktop",
  "version": "1.0.0",
  "private": true,
  "description": "Invoiz — Desktop Website — PHP/Laravel only, no Dart/Flutter, no mobile app tools",
  "scripts": {
    "start": "php backend/artisan serve --host=127.0.0.1 --port=8000",
    "dev": "php backend/artisan serve --host=127.0.0.1 --port=8000",
    "serve": "php backend/artisan serve --host=127.0.0.1 --port=8000"
  },
  "keywords": ["invoiz", "desktop", "php", "laravel"],
  "author": "Invoiz",
  "license": "MIT"
}
